import { Color } from "./color.type";

export interface ThemeColors {
  primary: Color;
  onPrimary: Color;
  success: Color;
  onSuccess: Color;
  warning: Color;
  onWarning: Color;
  danger: Color;
  onDanger: Color;
  info: Color;
  onInfo: Color;
}
