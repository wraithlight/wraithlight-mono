import { Position } from "./position.model";

/**
 * @deprecated Use `Sprite` from `@wraithlight/core.game.types` instead
 */
export interface Sprite {
    width: number;
    height: number;
    startAt: Position;
}
