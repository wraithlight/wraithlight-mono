import { Color } from "../color.enum";

export const enum CharacterHeadlineColor {
    SELF = Color.CHARACTER_HEADLINE_SELF,
    ALLY = Color.CHARACTER_HEADLINE_ALLY,
    NEUTRAL = Color.CHARACTER_HEADLINE_NEUTRAL,
    ENEMY = Color.CHARACTER_HEADLINE_ENEMY,
    UNALIVE = Color.CHARACTER_HEADLINE_UNALIVE,
}
