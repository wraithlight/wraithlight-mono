/**
 * Represents any nullable type that can be `T`, `null` or `undefined`.
 * @deprecated Use the same type from `"@wraithlight/framework.nullable"` instead.
 */
export type Nullable<T> = T | undefined | null;
