/**
 * @deprecated Use `RegisterRequest` from `core.auth.types` instead.
 */
export interface RegisterModel {
    username: string;
    emailAddress: string;
    password: string;
    passwordVerify: string;
}
