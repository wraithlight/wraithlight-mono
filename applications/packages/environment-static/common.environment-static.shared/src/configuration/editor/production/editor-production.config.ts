import { EditorShared } from "@wraithlight/core.environment-static.types";

export const SHARED_PRODUCTION_EDITOR_CONFIG: Readonly<
  EditorShared> = {
  server: {
    port: 4502,
    baseUrl: ""
  }
};
