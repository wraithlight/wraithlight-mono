import { ContentShared } from "@wraithlight/core.environment-static.types";

export const SHARED_LOCAL_CONTENT_CONFIG: Readonly<
  ContentShared> = {
  server: {
    port: 4501,
    baseUrl: "https://localhost"
  }
};
