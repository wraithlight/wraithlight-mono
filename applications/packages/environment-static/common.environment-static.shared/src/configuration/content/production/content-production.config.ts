import { ContentShared } from "@wraithlight/core.environment-static.types";

export const SHARED_PRODUCTION_CONTENT_CONFIG: Readonly<
  ContentShared> = {
  server: {
    port: 0,
    baseUrl: ""
  }
};
