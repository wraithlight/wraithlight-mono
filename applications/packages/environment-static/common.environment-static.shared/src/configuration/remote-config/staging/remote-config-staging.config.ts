import { RemoteConfigShared } from "@wraithlight/core.environment-static.types";

export const SHARED_STAGING_REMOTE_CONFIG_CONFIG: Readonly<
  RemoteConfigShared> = {
  server: {
    port: 0,
    baseUrl: ""
  }
};
