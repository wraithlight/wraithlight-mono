import { WebsiteShared } from "@wraithlight/core.environment-static.types";

export const SHARED_PRODUCTION_WEBSITE_CONFIG: Readonly<
  WebsiteShared> = {
  server: {
    port: 0,
    baseUrl: ""
  }
};
