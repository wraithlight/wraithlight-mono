import { UserManagementShared } from "@wraithlight/core.environment-static.types";

export const SHARED_STAGING_USER_MANAGEMENT_CONFIG: Readonly<
  UserManagementShared> = {
  server: {
    port: 0,
    baseUrl: ""
  }
};
