import { ForumShared } from "@wraithlight/core.environment-static.types";

export const SHARED_TEST_FORUM_CONFIG: Readonly<
  ForumShared> = {
  server: {
    port: 8080,
    baseUrl: "http://www.test.forum.wraithlight.ai"
  }
};
