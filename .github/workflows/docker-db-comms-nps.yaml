name: "Database GHA - Comms NPS Build"
run-name: "DB CNPS BUILD"
on: [push, workflow_dispatch]

env:
  WORK_DIR: database
  SRC_NAME: communications-nps
  DOCKER_FILE_PATH: Dockerfile
  DOCKER_IMAGE_NAME: db-comms-nps

jobs:
  dbCNPSBuild:
    name: Build COMMS NPS Docker Image
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build the Docker image
      working-directory: ${{ env.WORK_DIR }}
      run: docker build . -f ${{ env.DOCKER_FILE_PATH }} --build-arg SRC_NAME=$SRC_NAME -t $DOCKER_IMAGE_NAME

    - name: Remove the local image
      run: docker rmi $DOCKER_IMAGE_NAME
